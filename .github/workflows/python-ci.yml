name: Python CI

on: push

permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3-dev \
            libpq-dev \
            libhdf5-dev \
            libnetcdf-dev \
            gdal-bin \
            libgdal-dev \
            libproj-dev \
            proj-data \
            libgeos-dev

      - name: Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          export PATH="$HOME/.local/bin:$PATH"
          poetry --version

      - name: Install project dependencies (including test extras)
        env:
          CPLUS_INCLUDE_PATH: /usr/include/gdal
          C_INCLUDE_PATH: /usr/include/gdal
        run: poetry install --extras "test"

      - name: Test with pytest (full)
        if: github.ref == 'refs/heads/master'
        run: poetry run pytest tests/ -m "not online" --cov --cov-report term-missing

      - name: Test with pytest (fast)
        if: github.ref != 'refs/heads/master'
        run: poetry run pytest -m "not slow and not online"
